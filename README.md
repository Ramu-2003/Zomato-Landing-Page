# MERN STACK DAY - 18 --- 12/01/2026

# DAY - 18 || Git and Zomato Landing Page || Part - 1

# git commands

# if the commands were not work then we should first write these two commands :

# >> git config --global user.name "Ramu-2003"

# >> git config --global user.email "ramu4012y@email.com"

# >> git init

# >> git status

# >> git add . [or] git add index.html styles.css

# >> git remote add origin https://github.com/Ramu-2003/Zomato-Landing-Page.git

# >> git commit -m "any message"

# >> git branch -m master [or] main

# >> git push -u origin master [or] main

# >> if git master has failed then git --set-upstream origin master

# >> if we have done changes in github and we want to pull to vs then we should use :

# git pull origin master { then it get pulled from github to vs code}

# git checkout -b  branch name

#       ex : git checkout -b setup {now setup will become the another branch}
